services:
  - type: web
    name: lichhoc-api
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn requirements.schedule_api:app --bind 0.0.0.0:$PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
